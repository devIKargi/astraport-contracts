[package]
name = "astraport-rebalancing"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
astraport-audit = { path = "../audit" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
